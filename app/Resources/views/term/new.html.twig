{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="container">
            
            <h2 class="page-header">Term creation</h2>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('admin_taxonomy_term_index') }}">Back to the list</a>
                </li>
            </ul>

        </div>
    </div>
{% endblock %}
